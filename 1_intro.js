const amount = 9;
const wallNum = 10;

amount < wallNum ? console.log(`${amount} is less than ${wallNum}`) : console.log(`${amount} is less than ${wallNum}`);

console.log('Hey it\'s my first node app');